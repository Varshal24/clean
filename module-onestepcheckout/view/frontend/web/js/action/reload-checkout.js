define(
    [],
    function () {
        'use strict';

        return {
            /**
             * Reload current checkout page
             */
            execute: function () {
                window.location.reload();
            }
        };
    }
);
